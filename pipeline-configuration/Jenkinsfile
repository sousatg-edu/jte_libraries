pipeline {
    agent none
    stages {
        stage("Example Build") {
            steps {
                echo 'Example Build'
            }
        }

        @override stage("Example Deploy") {
            steps {
                echo 'Example Deploy'
            }
        }
    }
}
