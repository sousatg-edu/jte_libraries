pipeline {
    agent none
    stages {
        stage("Example Build") {
            steps {
                echo 'Example Build'
            }
        }

        stage("Example Deploy") {
            steps {
                echo 'Example Deploy'
            }
        }
    }
}
